import { FC } from 'react';
interface Props {
    x?: number;
    y?: number;
    color: string;
    hue: string;
    isLocalClient?: boolean;
    userId: string;
}
export declare const Cursor: FC<Props>;
export {};
